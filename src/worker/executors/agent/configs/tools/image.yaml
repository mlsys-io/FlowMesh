name: image
mode: builtin
activated_tools: null
config:
  timeout: 15
config_llm:
  model_provider:
    # dummy! We actually use `UTU_IMAGE_LLM_*`
    type: ${oc.env:UTU_LLM_TYPE}
    model: ${oc.env:UTU_LLM_MODEL}
    api_key: ${oc.env:UTU_LLM_API_KEY}
    base_url: ${oc.env:UTU_LLM_BASE_URL}